const nextConfig = {
    output: 'export', // static export for GitHub Pages
    images: { unoptimized: true },
    trailingSlash: true // ensures folder-style URLs for Pages
  };
  export default nextConfig;